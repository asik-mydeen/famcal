# Project Intelligence: FamCal (Family Calendar App)

> Auto-generated by deep-research. Last updated: 2026-03-24. Last incremental update: 2026-03-24
> Re-run `/deep-research` to refresh after major changes.

## Quick Reference
- **Stack:** React (plain JS) / MUI 5.13.1 / CRA 5.0.1 / Supabase / Vercel AI SDK
- **Entry:** `src/index.js` → AuthProvider → ThemeModeProvider → FamilyProvider → App
- **Build:** `npm run build` | Test: `npm test` | Dev: `npm start` | Desktop: `npm run tauri:dev`
- **Deploy:** Vercel (auto) | Tauri desktop builds via GitHub Actions
- **Live:** https://calendar-app-01.vercel.app

## Architecture

**Kiosk-first family app** — designed for wall-mounted touch displays. NO Sidenav, NO DashboardLayout, NO DashboardNavbar (all removed from original Argon template).

**Provider hierarchy:**
```
BrowserRouter → AuthProvider → ThemeModeProvider → FamilyProvider → App → TimerAlarmProvider
```

**App shell (App.js):**
```
KioskWrapper
├── HeaderBar (sticky: date, weather, countdown, avatars, clock)
├── TabStrip (desktop horizontal nav — hidden on mobile)
├── Routes (AnimatePresence + PageTransition per route)
├── FloatingNav (mobile bottom nav)
├── AI FAB → AIAssistant (slide panel)
└── TimerAlarmPanel (slide panel)
```

## Active Routes

| Path | Component | Features |
|------|-----------|----------|
| `/calendar` | FamilyCalendar | Day/week/month views, Google Calendar sync, event CRUD, notes |
| `/chores` | Chores | Grid/list view, categories, priority, recurring, gamification |
| `/meals` | Meals | Weekly grid, AI meal generation, dietary prefs |
| `/lists` | Lists | Multi-list, auto-categorize groceries, checkboxes |
| `/family` | Family | Member CRUD, avatar upload, Google Calendar connect, stats |
| `/rewards` | Rewards | Claim rewards, leaderboard, gamification overview |
| `/settings` | Settings | All config: Supabase, OAuth, weather, fonts, kiosk, photos |
| `/d/:slug` | Dashboard | Public kiosk (token auth, no Google login) |
| `/tasks` | → `/chores` | Redirect (deprecated) |

## State Management (4 Contexts)

### FamilyContext — `useFamilyController()` → `[state, dispatch]`
- **48+ action types** covering: family, members, tasks, events, rewards, meals, lists, notes, countdowns, photos, weather, AI preferences/conversations/memories
- **Persistence:** Optimistic local updates → async Supabase writes (fire-and-forget)
- **Temp ID pattern:** `"task-${Date.now()}"` → replaced with real UUID after Supabase insert
- **Constants:** `MEMBER_COLORS` (8), `TASK_CATEGORIES` (7)

### AuthContext — `useAuth()` → `{ user, loading, signIn, signOut, providerToken }`
- Supabase Auth with Google OAuth (Calendar + Photos scopes)

### ThemeContext — `useThemeMode()` → `{ darkMode, toggleDarkMode }`
- Persists to localStorage `famcal_dark_mode`

### TimerAlarmContext — `useTimerAlarm()`
- Timers: in-memory only | Alarms: Supabase-persisted
- Alert sound: Web Audio API (800Hz sine wave)

## Color System Audit (CRITICAL — Theme Engine Prep)

### Theme Palette Tokens (src/assets/theme/index.js)
| Token | Light | Dark | Usage |
|-------|-------|------|-------|
| primary | `#6C5CE7` | `#8b5cf6` | Accent, buttons, active states |
| secondary | `#00B894` | `#22d3ee` | Secondary actions |
| success | `#22c55e` | `#22c55e` | Completion, positive |
| error | `#E17055` | `#E17055` | Errors, high priority |
| warning | `#FDCB6E` | `#FDCB6E` | Warnings, medium |
| info | `#0984E3` | `#3b82f6` | Informational, blue |
| background.default | `transparent` | `#13131f` | — |
| background.paper | `#ffffff` | `#1c1c2e` | — |
| text.primary | `#1A1A1A` | `#f0f0f5` | — |
| text.secondary | `#8B8680` | `rgba(240,240,245,0.65)` | — |

### Hardcoded Colors (200+ instances — NOT using theme tokens)
| Category | Colors | Files |
|----------|--------|-------|
| Purple variants | `#7c3aed`, `#a855f7`, `#6d28d9`, `#9333ea` | chores, meals, lists, rewards |
| Orange/amber | `#f59e0b`, `#fbbf24`, `#d97706` | tasks, chores (priority) |
| Rose/red | `#f43f5e`, `#ef4444`, `#e11d48` | tasks, chores (high priority) |
| Teal (meals) | `#4ECDC4`, `#44a6a0`, `#0f766e` | meals page only |
| Slate gray | `#64748b`, `#94a3b8`, `#cbd5e1` | tasks, chores (other) |
| Countdown palette | 8 hardcoded colors | CountdownWidget |

### Priority Color Inconsistency
- **chores:** high=`#ef4444`, medium=`#f59e0b`, low=`#22c55e`
- **tasks:** high=`#f43f5e`, medium=`#f59e0b`, low=`rgba(0,0,0,0.15)`

### Gradient Patterns (12 unique, no centralized tokens)
- **Primary:** `linear-gradient(135deg, #6C5CE7, #A29BFE)` (light) / `#8b5cf6, #a855f7` (dark)
- **Success/Error/Info/Warning:** Each has own gradient pair
- **Meals/Weather/Kiosk:** Unique gradients per component

### Missing Token Categories Needed
```
palette.meals, palette.priority, palette.category (7), palette.countdown (8),
palette.level (3), palette.gradients (12+), palette.glass (bg/border/shadow)
```

## Typography System
- **Primary font:** Inter (configurable: Open Sans, Roboto, Poppins, Montserrat, System)
- **Font scale:** 0.9x–1.7x (localStorage, applied globally)
- **Theme function:** `createAppTheme(mode, fontFamily)` in `src/assets/theme/index.js`
- **CDN fonts:** Inter, Quicksand, Dancing Script, Playfair Display, Caveat, Sacramento, Nunito, Poppins

## External Integrations

| Service | Module | Auth | Status |
|---------|--------|------|--------|
| **Supabase** | `lib/supabase.js` | Anon key + RLS | Full CRUD (16+ tables) |
| **Google Calendar** | `lib/googleCalendar.js` | OAuth per-member | Two-way sync |
| **Google Photos** | `lib/googlePhotos.js` | Provider token | Album fetch via proxy |
| **OpenWeather** | `lib/weather.js` | API key | Current + 3-day forecast |
| **OpenAI (via Vercel AI SDK)** | `lib/ai.js` | API endpoint `/api/chat` | Streaming + tool calls |
| **Vercel Functions** | `api/` directory | — | chat, photos, dashboard |

## Supabase Tables
`families`, `family_members`, `tasks`, `events`, `rewards`, `meals`, `lists`, `list_items`, `notes`, `countdowns`, `photos`, `alarms`, `ai_preferences`, `conversations`, `conversation_messages`, `ai_memories`

## Key Components

| Component | Purpose |
|-----------|---------|
| **PageShell** | Consistent page padding wrapper (replaces DashboardLayout) |
| **GlassCard** | Primary card — glassmorphism, motion, hover effects |
| **SlidePanel** | Modal replacement — slides from right, fullscreen mobile |
| **HeaderBar** | Top bar — date, weather, countdown, avatars, clock |
| **TabStrip** | Desktop horizontal tabs (7 routes) |
| **FloatingNav** | Mobile bottom nav |
| **AIAssistant** | "Amara" AI chat — context-aware, action execution |
| **PhotoFrame** | Idle slideshow (Google Photos) |
| **ChoreGrid/MealGrid** | Weekly calendar grids |

## Gamification Model
- **Points:** `task.points_value` awarded on completion (default 10)
- **Levels:** `Math.floor(points / 100) + 1` → Beginner to Supreme
- **Rewards:** Claimable items, deducts `points_cost` from member balance
- **Streaks:** `streak_days` tracked (not auto-incremented yet)

## Test Infrastructure
- **Framework:** Jest (CRA default) + @testing-library/react
- **Tests:** **ZERO** project tests (0/52 source files)
- **CI:** GitHub Actions for Tauri desktop builds only (no test/lint step)
- **Lint:** ESLint + Prettier configured but no `npm run lint` script

## Critical Paths (Most Important Files)
1. `src/App.js` — App shell, routing, providers (744 lines)
2. `src/context/FamilyContext.js` — Domain state, 48+ actions (916 lines)
3. `src/assets/theme/index.js` — Theme composition, `createAppTheme()`
4. `src/lib/supabase.js` — All Supabase CRUD operations
5. `src/components/GlassCard/index.js` — Primary card component
6. `src/components/SlidePanel/index.js` — Modal replacement
7. `src/components/HeaderBar/index.js` — Top bar (18 hardcoded colors)
8. `src/context/AuthContext.js` — Google OAuth flow
9. `src/lib/ai.js` — AI assistant backend
10. `src/lib/googleCalendar.js` — Two-way calendar sync

## Environment Variables
| Variable | Required | Purpose |
|----------|----------|---------|
| `REACT_APP_SUPABASE_URL` | No | Supabase project URL |
| `REACT_APP_SUPABASE_ANON_KEY` | No | Supabase anon key |
| `REACT_APP_GOOGLE_CLIENT_ID` | No | Google OAuth |
| `REACT_APP_WEATHER_API_KEY` | No | OpenWeather API |
| `REACT_APP_SITE_URL` | No | OAuth redirect URL |

## Known Gotchas
1. **`--openssl-legacy-provider`** required for CRA 5.0.1 with newer Node.js
2. **`--legacy-peer-deps`** required for npm install
3. **200+ hardcoded hex colors** bypass theme system — theme engine must centralize these
4. **Priority colors inconsistent** between tasks and chores pages
5. **Opacity pattern:** hex suffix (`${color}18`), NOT `rgba()` — theme engine must support this
6. **No DashboardLayout/Sidenav** — removed from template. Use PageShell + PageTransition
7. **Temp IDs** (`task-${Date.now()}`) replaced with UUIDs after Supabase insert
8. **Gold text contrast:** `#FDCB6E` needs dark text (`#1a1a1a`) for accessibility
9. **Absolute imports only** — CRA configured with `src/` base path
10. **Icon fontSize:** Always use `!important` (MUI override issue)
